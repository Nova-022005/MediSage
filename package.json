{
  "name": "medisage",
  "version": "1.0.0",
  "description": "Medical Document Management System with AI Analysis",
  "main": "server/server.js",
  "scripts": {
    "install-all": "npm install && cd client && npm install",
    "server": "node server/server.js",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm run build",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": ["medical", "document-management", "ai", "langchain", "mern"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "langchain": "^0.0.150",
    "@langchain/openai": "^0.0.10",
    "concurrently": "^8.2.1"
  }
}
